const config = {
    routes: {
        home: "/",
        products: "/products",
        productDetail: "/products/:id",
        blog: "/blog",
        learning: "/learning-paths",
        notFound: "*",
    },
};

export default config;
